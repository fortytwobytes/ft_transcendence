---
services:
  transcendence:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - .:/code/
    command: ["python", "manage.py", "runserver", "0.0.0.0:8080"]
    tty: true
